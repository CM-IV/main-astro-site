---
import Author from './author.astro';

export interface Props {
  title: string;
  author: string;
  pubDate: string;
}

const { title, pubDate } = Astro.props;

console.log(title)
---
<section>
  <article>
    <div>
      <header>
        <p>{pubDate}</p>
        <h1>{title}</h1>
        <Author name="CM-IV" />
      </header>
      <slot />
    </div>
  </article>
</section>